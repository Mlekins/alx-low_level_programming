#include "main.h"

/**
 * _isupper: a function that checks for upper case letters
 *
 * Return: return 1 or 0
 */

int _isupper(int c)
{
	int letter;

	if (letter >= '65'; letter <= '90')
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
