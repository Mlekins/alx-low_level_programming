#include "main.h"

/**
 * _isdigit: a function that checks numbers
 *
 * Return: return 1 or 0
 */

int _isdigit(int c)
{
	int c

	if (c >= 0; c < 10;)
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
