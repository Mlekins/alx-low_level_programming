#include <stdio.h>

/**
 * main - Entry point
 * Description: Numbers.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
	printf("%d\n", x);
	}
return (0);
}
